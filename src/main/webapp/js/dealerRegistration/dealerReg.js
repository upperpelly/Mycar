var isChecked1 = false;

        function allSelected1() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked1){
            document.getElementById("newCarVehDeal").value=true;
            isChecked1 = !isChecked1;
        	}
        	else{
        		document.getElementById("newCarVehDeal").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked2= false;

        function allSelected2() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked2){
            document.getElementById("usedCarVehDeal").value=true;
            isChecked2 = !isChecked2;
        	}
        	else{
        		document.getElementById("usedCarVehDeal").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        
        var isChecked3 = false;

        function allSelected3() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked3){
            document.getElementById("bothCarVehDeal").value=true;
            isChecked3 = !isChecked3;
        	}
        	else{
        		document.getElementById("bothCarVehDeal").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        
        var isChecked4 = false;

        function allSelected4() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked4){
            document.getElementById("indivFL").value=true;
            isChecked4 = !isChecked4;
        	}
        	else{
        		document.getElementById("indivFL").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked5 = false;

        function allSelected5() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked5){
            document.getElementById("licFL").value=true;
            isChecked5 = !isChecked5;
        	}
        	else{
        		document.getElementById("licFL").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        
        var isChecked6 = false;

        function allSelected6() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked6){
            document.getElementById("finFL").value=true;
            isChecked6 = !isChecked6;
        	}
        	else{
        		document.getElementById("finFL").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        } 
        
        
        var isChecked7 = false;

        function allSelected7() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked7){
            document.getElementById("newCarFL").value=true;
            isChecked7 = !isChecked7;
        	}
        	else{
        		document.getElementById("newCarFL").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        
        var isChecked8 = false;

        function allSelected8() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked8){
            document.getElementById("usedCarFL").value=true;
            isChecked8 = !isChecked8;
        	}
        	else{
        		document.getElementById("usedCarFL").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked9 = false;

        function allSelected9() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked9){
            document.getElementById("bothCarFL").value=true;
            isChecked9 = !isChecked9;
        	}
        	else{
        		document.getElementById("bothCarFL").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        
        var isChecked10 = false;

        function allSelected10() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked10){
            document.getElementById("newCarSM").value=true;
            isChecked10 = !isChecked10;
        	}
        	else{
        		document.getElementById("newCarSM").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked11 = false;

        function allSelected11() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked11){
            document.getElementById("usedCarSM").value=true;
            isChecked11= !isChecked11;
        	}
        	else{
        		document.getElementById("usedCarSM").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        
        var isChecked12 = false;

        function allSelected12() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked12){
            document.getElementById("bothCarSM").value=true;
            isChecked12 = !isChecked12;
        	}
        	else{
        		document.getElementById("bothCarSM").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked13 = false;

        function allSelected13() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked13){
            document.getElementById("spareAcc").value=true;
            isChecked13 = !isChecked13;
        	}
        	else{
        		document.getElementById("spareAcc").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        
        
        var isChecked14 = false;

        function allSelected14() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked14){
            document.getElementById("allAusTrans").value=true;
            isChecked14 = !isChecked14;
        	}
        	else{
        		document.getElementById("allAusTrans").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        } 
        
        var isChecked15 = false;

        function allSelected15() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked15){
            document.getElementById("act").value=true;
            isChecked15 = !isChecked15;
        	}
        	else{
        		document.getElementById("act").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        
        var isChecked16 = false;

        function allSelected16() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked16){
            document.getElementById("nsw").value=true;
            isChecked16 = !isChecked16;
        	}
        	else{
        		document.getElementById("nsw").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        
        
        var isChecked17 = false;

        function allSelected17() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked17){
            document.getElementById("vic").value=true;
            isChecked17 = !isChecked17;
        	}
        	else{
        		document.getElementById("vic").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        
        var isChecked18 = false;

        function allSelected18() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked18){
            document.getElementById("qld").value=true;
            isChecked18 = !isChecked18;
        	}
        	else{
        		document.getElementById("qld").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        
        var isChecked19 = false;

        function allSelected19() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked19){
            document.getElementById("tas").value=true;
            isChecked19 = !isChecked19;
        	}
        	else{
        		document.getElementById("tas").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked20 = false;

        function allSelected20() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked20){
            document.getElementById("sa").value=true;
            isChecked20 = !isChecked20;
        	}
        	else{
        		document.getElementById("sa").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        
        
        var isChecked21 = false;

        function allSelected21() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked21){
            document.getElementById("nt").value=true;
            isChecked21 = !isChecked21;
        	}
        	else{
        		document.getElementById("nt").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked22 = false;

        function allSelected22() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked22){
            document.getElementById("wa").value=true;
            isChecked22 = !isChecked22;
        	}
        	else{
        		document.getElementById("wa").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked23 = false;

        function allSelected23() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked23){
            document.getElementById("allAusAop").value=true;
            isChecked23 = !isChecked23;
        	}
        	else{
        		document.getElementById("allAusAop").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked24 = false;

        function allSelected24() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked24){
            document.getElementById("actAOP").value=true;
            isChecked24 = !isChecked24;
        	}
        	else{
        		document.getElementById("actAOP").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked25 = false;

        function allSelected25() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked25){
            document.getElementById("nswAOP").value=true;
            isChecked25 = !isChecked25;
        	}
        	else{
        		document.getElementById("nswAOP").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked26 = false;

        function allSelected26() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked26){
            document.getElementById("vicAOP").value=true;
            isChecked26 = !isChecked26;
        	}
        	else{
        		document.getElementById("vicAOP").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked27 = false;

        function allSelected27() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked27){
            document.getElementById("qldAOP").value=true;
            isChecked27 = !isChecked27;
        	}
        	else{
        		document.getElementById("qldAOP").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked28 = false;

        function allSelected28() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked28){
            document.getElementById("tasAOP").value=true;
            isChecked28 = !isChecked28;
        	}
        	else{
        		document.getElementById("tasAOP").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked29 = false;

        function allSelected29() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked29){
            document.getElementById("saAOP").value=true;
            isChecked29 = !isChecked29;
        	}
        	else{
        		document.getElementById("saAOP").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked30 = false;

        function allSelected30() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked30){
            document.getElementById("ntAOP").value=true;
            isChecked30 = !isChecked30;
        	}
        	else{
        		document.getElementById("ntAOP").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }

        var isChecked30 = false;

        function allSelected31() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked31){
            document.getElementById("waAOP").value=true;
            isChecked31 = !isChecked31;
        	}
        	else{
        		document.getElementById("waAOP").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }
        var isChecked32 = false;

        function allSelected32() 
        {
           //alert("inside check box");
        	// this line is for toggle the check
            //isChecked = !isChecked;
        	if(!isChecked32){
            document.getElementById("allMakes").value=true;
            isChecked32 = !isChecked32;
        	}
        	else{
        		document.getElementById("allMakes").value=false;
        	}
        		
            //below line refers to 'jpCheckbox' class
            //$('input:checkbox.jpCheckbox').attr('checked',isChecked);

            //OR,
            //$('input:checkbox.jpCheckbox').attr('checked','checked');
        }

        
     // append row to the HTML table
        function appendRow17() {
            var tbl = document.getElementById('my-table17'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellCountry(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }// append row to the HTML table
        function appendRow16() {
            var tbl = document.getElementById('my-table16'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellExpertingRating(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }// append row to the HTML table
        function appendRow15() {
            var tbl = document.getElementById('my-table15'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellUserRating(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
            alert("ca"+$('#userRating10').val());
        }// append row to the HTML table
        function appendRow14() {
            var tbl = document.getElementById('my-table14'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellFittingInternal(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }// append row to the HTML table
        function appendRow13() {
            var tbl = document.getElementById('my-table13'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellFittingExternal(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }// append row to the HTML table
        function appendRow12() {
            var tbl = document.getElementById('my-table12'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellVideos(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }// append row to the HTML table
        function appendRow11() {
            var tbl = document.getElementById('my-table11'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellVehicleReviews(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }
     // append row to the HTML table
        function appendRow10() {
            var tbl = document.getElementById('my-table10'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellPhotos(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }
     // append row to the HTML table
        function appendRow9() {
        	//alert("append invoke");
            var tbl = document.getElementById('my-table9'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellRelatedModels(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }
     // append row to the HTML table
        function appendRow91() {
        	//alert("append invoke");
            var tbl = document.getElementById('my-table91'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellRelatedModels91(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }
     // append row to the HTML table
        function appendRow922() {
        	//alert("append invoke");
            var tbl = document.getElementById('my-table922'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellRelatedModels922(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }
     // append row to the HTML table
        function appendRow8() {
            var tbl = document.getElementById('my-table8'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellOptionalEquipmentAddOns(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }
     // append row to the HTML table
        function appendRow7() {
            var tbl = document.getElementById('my-table7'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellFullSpecStandardEquip(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }
     // append row to the HTML table
        function appendRow6() {
            var tbl = document.getElementById('my-table6'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellWarranty(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }
     // append row to the HTML table
        function appendRow5() {
            var tbl = document.getElementById('my-table5'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellFullSpecWheels(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }
     // append row to the HTML table
        function appendRow4() {
            var tbl = document.getElementById('my-table4'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellFullSpecFuel(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }
     // append row to the HTML table
        function appendRow3() {
            var tbl = document.getElementById('my-table3'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellFullSpecDimension(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }
     // append row to the HTML table
        function appendRow2() {
            var tbl = document.getElementById('my-table2'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellFullSpecEngine(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }
        
     // append row to the HTML table
        function appendRow1() {
            var tbl = document.getElementById('my-table1'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellAvailableColour(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
        }
        function appendRow() {
            var tbl = document.getElementById('my-table'), // table reference
                row = tbl.insertRow(tbl.rows.length),      // append table row
                i;
            // insert table cells to the new row
            for (i = 0; i < tbl.rows[0].cells.length; i++) {
            	createCellOptionalFeaturesAddons(row.insertCell(i), tbl.rows.length-1, i, 'row');
            }
            alert("ca"+$('#createCellOptionalFeaturesAddons2').val());
        }
        // create DIV element and append to the table cell
        function createCellOptionalFeaturesAddons(cell, row, text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "createCellOptionalFeaturesAddons" +row + text;
            newInput.name = "createCellOptionalFeaturesAddons" +row+ text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            ////div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        
        // create DIV element and append to the table cell
        function createCellAvailableColour(cell, row, text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "availableColour" + +row + text;
            newInput.name = "availableColour" +row +  text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            ////div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        // create DIV element and append to the table cell
        function createCellFullSpecEngine(cell,  row, text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "fullSpecEngine" + row +text;
            newInput.name = "fullSpecEngine" + row +text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            ////div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        // create DIV element and append to the table cell
        function createCellFullSpecDimension(cell,  row, text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "fullSpecDimension" + row+text;
            newInput.name = "fullSpecDimension" + row+text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            ////div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        // create DIV element and append to the table cell
        function createCellFullSpecFuel(cell,  row, text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "fullSpecFuel" + row+text;
            newInput.name = "fullSpecFuel" + row+text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            ////div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        // create DIV element and append to the table cell
        function createCellFullSpecWheels(cell,  row,text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "fullSpecWheels" + row+text;
            newInput.name = "fullSpecWheels" + row+text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            ////div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        // create DIV element and append to the table cell
        function createCellWarranty(cell,  row,text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "warranty" + row+text;
            newInput.name = "warranty" + row+text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            //div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        // create DIV element and append to the table cell
        function createCellFullSpecStandardEquip(cell,  row,text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "fullSpecStandardEquip" + row+text;
            newInput.name = "fullSpecStandardEquip" + row+text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            //div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        // create DIV element and append to the table cell
        function createCellOptionalEquipmentAddOns(cell,  row,text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "optionalEquipmentAddOns" +row+ text;
            newInput.name = "optionalEquipmentAddOns" + row+text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            //div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        // create DIV element and append to the table cell
        //createCellRelatedModels(row.insertCell(i), tbl.rows.length-1, i, 'row');
        function createCellRelatedModels(cell,  row, text, style) {
        	//alert("came here1214"+$('#car-years').val());
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "relatedModels" +row+ text;
            newInput.name = "relatedModels" + row+text;
            newInput.type = "text";
            switch(text){
            case 0: newInput.value=$('#car-years').val();break;
            case 1: newInput.value=$('#car-makes').val();break;
            case 2: newInput.value=$('#car-models').val();break;
            case 3: newInput.value=$('#car-model-trims').val();break;
            }
            
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            //div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
     // create DIV element and append to the table cell
        //createCellRelatedModels(row.insertCell(i), tbl.rows.length-1, i, 'row');
        function createCellRelatedModels91(cell,  row, text, style) {
        	//alert("came here1214"+$('#car-years').val());
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "relatedModels91" +row+ text;
            newInput.name = "relatedModels91" + row+text;
            newInput.type = "text";
            switch(text){
            case 0: newInput.value=$('#car-yearsreg').val();break;
            case 1: newInput.value=$('#car-makesreg').val();break;
            case 2: newInput.value=$('#car-modelsreg').val();break;
            }
            
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            //div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
     // create DIV element and append to the table cell
        //createCellRelatedModels(row.insertCell(i), tbl.rows.length-1, i, 'row');
        function createCellRelatedModels922(cell,  row, text, style) {
        	//alert("came here1214"+$('#car-years').val());
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "relatedModels922" +row+ text;
            newInput.name = "relatedModels922" + row+text;
            newInput.type = "text";
            switch(text){
            case 0: newInput.value=$('#car-makes922').val();break;
            case 1: newInput.value="YES";break;
            }
            
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            //div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        // create DIV element and append to the table cell
        function createCellPhotos(cell,  row,text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "photo" + row+text;
            newInput.name = "photo" + row+text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            //div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        // create DIV element and append to the table cell
        function createCellVehicleReviews(cell,  row, text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "vehicleReviews" + row+text;
            newInput.name = "vehicleReviews" + row+text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            //div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        // create DIV element and append to the table cell
        function createCellVideos(cell,  row, text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "videos" + row+text;
            newInput.name = "videos" + row+text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            //div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        // create DIV element and append to the table cell
        function createCellFittingExternal(cell,  row, text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "fittingExternal" + row+text;
            newInput.name = "fittingExternal" + row+text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            //div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        // create DIV element and append to the table cell
        function createCellFittingInternal(cell,  row, text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "fittingInternal" + row+text;
            newInput.name = "fittingInternal" + row+text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            //div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        // create DIV element and append to the table cell
        function createCellUserRating(cell,  row, text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "userRating" + row+ text;
            newInput.name = "userRating" + row+ text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            //div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        // create DIV element and append to the table cell
        function createCellExpertingRating(cell,  row, text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "expertingRating" + row+ text;
            newInput.name = "expertingRating" + row+ text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            //div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        // create DIV element and append to the table cell
        function createCellCountry(cell,  row, text, style) {
            var div = document.createElement('div'); // create DIV element
                //txt = document.createTextNode(text); // create text node
            
            var newInput = document.createElement("INPUT");
            newInput.id = "country" + row+ text;
            newInput.name = "country" + row+ text;
            newInput.type = "text";
            
            div.appendChild(newInput);                    // append text node to the DIV
            div.setAttribute('class', style);        // set DIV class attribute
            //div.setAttribute('className', style);    // set DIV class attribute for IE (?!)
            cell.appendChild(div);                   // append DIV to the table cell
        }
        
        
        
        
     // append column to the HTML table
        function appendColumn() {
            var tbl = document.getElementById('my-table'), // table reference
                i;
            // open loop for each row and append cell
            for (i = 0; i < tbl.rows.length; i++) {
                createCell(tbl.rows[i].insertCell(tbl.rows[i].cells.length), i, 'col');
            }
        }
        
        // delete table rows with index greater then 0
        function deleteRows1() {
            var tbl = document.getElementById('my-table1'), // table reference
                lastRow = tbl.rows.length - 1,             // set the last row index
                i;
            // delete rows with index greater then 0
            for (i = lastRow; i > 0; i--) {
                tbl.deleteRow(i);
            }
        }
        function deleteRows2() {
            var tbl = document.getElementById('my-table2'), // table reference
                lastRow = tbl.rows.length - 1,             // set the last row index
                i;
            // delete rows with index greater then 0
            for (i = lastRow; i > 0; i--) {
                tbl.deleteRow(i);
            }
        }
        function deleteRows3() {
            var tbl = document.getElementById('my-table3'), // table reference
                lastRow = tbl.rows.length - 1,             // set the last row index
                i;
            // delete rows with index greater then 0
            for (i = lastRow; i > 0; i--) {
                tbl.deleteRow(i);
            }
        }
        function deleteRows4() {
            var tbl = document.getElementById('my-table4'), // table reference
                lastRow = tbl.rows.length - 1,             // set the last row index
                i;
            // delete rows with index greater then 0
            for (i = lastRow; i > 0; i--) {
                tbl.deleteRow(i);
            }
        }
        function deleteRows5() {
            var tbl = document.getElementById('my-table5'), // table reference
                lastRow = tbl.rows.length - 1,             // set the last row index
                i;
            // delete rows with index greater then 0
            for (i = lastRow; i > 0; i--) {
                tbl.deleteRow(i);
            }
        }
        function deleteRows6() {
            var tbl = document.getElementById('my-table6'), // table reference
                lastRow = tbl.rows.length - 1,             // set the last row index
                i;
            // delete rows with index greater then 0
            for (i = lastRow; i > 0; i--) {
                tbl.deleteRow(i);
            }
        }
        function deleteRows7() {
            var tbl = document.getElementById('my-table7'), // table reference
                lastRow = tbl.rows.length - 1,             // set the last row index
                i;
            // delete rows with index greater then 0
            for (i = lastRow; i > 0; i--) {
                tbl.deleteRow(i);
            }
        }
        function deleteRows8() {
            var tbl = document.getElementById('my-table8'), // table reference
                lastRow = tbl.rows.length - 1,             // set the last row index
                i;
            // delete rows with index greater then 0
            for (i = lastRow; i > 0; i--) {
                tbl.deleteRow(i);
            }
        }
        function deleteRows9() {
            var tbl = document.getElementById('my-table9'), // table reference
                lastRow = tbl.rows.length - 1,             // set the last row index
                i;
            // delete rows with index greater then 0
            for (i = lastRow; i > 0; i--) {
                tbl.deleteRow(i);
            }
        }
        function deleteRows10() {
            var tbl = document.getElementById('my-table10'), // table reference
                lastRow = tbl.rows.length - 1,             // set the last row index
                i;
            // delete rows with index greater then 0
            for (i = lastRow; i > 0; i--) {
                tbl.deleteRow(i);
            }
        }
     // delete table rows with index greater then 0
        function deleteRows() {
            var tbl = document.getElementById('my-table'), // table reference
                lastRow = tbl.rows.length - 1,             // set the last row index
                i;
            // delete rows with index greater then 0
            for (i = lastRow; i > 0; i--) {
                tbl.deleteRow(i);
            }
        }
         
        // delete table columns with index greater then 0
        function deleteColumns() {
            var tbl = document.getElementById('my-table'), // table reference
                lastCol = tbl.rows[0].cells.length - 1,    // set the last column index
                i, j;
            // delete cells with index greater then 0 (for each row)
            for (i = 0; i < tbl.rows.length; i++) {
                for (j = lastCol; j > 0; j--) {
                    tbl.rows[i].deleteCell(j);
                }
            }
        }





//angular JS start
var mainApp1 = angular.module("mainApp1d", []);
mainApp1.controller(
				'myController1d',function($scope, $http) {
					$scope.firstForm = function() {
						alert("in first form");
						$('#first').removeClass('disabled');
				        $('#first1').attr('data-toggle','pill');
				        document.getElementById("first1").click();
				        //$('#menu1').show();
				        //$('a#first1').click();
					};
					
					$scope.secondForm = function() {
						alert("in second form");
						$('#second').removeClass('disabled');
				        $('#second1').attr('data-toggle','pill');
				        document.getElementById("second1").click();
				        //$('#second1').click();
				        //$('#menu2').show();
					};
					
					$scope.thirdForm = function() {
						alert("in third form");
						$('#third').removeClass('disabled');
				        $('#third1').attr('data-toggle','pill');
				        document.getElementById("third1").click();	        
					};
					
					$scope.loginDealerForm = function() {
						alert("in Delaer form");

						var jsonInputToAPI = {"email":"","password":""};
						jsonInputToAPI.email=document.getElementById("dealerEmail").value;
						jsonInputToAPI.password=document.getElementById("dealerPass").value;
						var wsURL = 'api/dealer/login';
						
					    $http({
									method : 'POST',
									url : wsURL,
									data: jsonInputToAPI
													
								}).success(function(data) {
									alert("Successfully Logged In");	
												});	
					};
					
					$scope.submitSearchForm = function() {
						alert("came here");
						var newCarVehDeal=false;
						newCarVehDeal = document.getElementById("newCarVehDeal").value;
						if(newCarVehDeal=="on")newCarVehDeal=false;
						var usedCarVehDeal=false;
						usedCarVehDeal = document.getElementById("usedCarVehDeal").value;
						if(newCarVehDeal=="on")newCarVehDeal=false;
						var bothCarVehDeal=false;
						bothCarVehDeal = document.getElementById("bothCarVehDeal").value;
						if(newCarVehDeal=="on")newCarVehDeal=false;
						var indivFL=false;
						indivFL = document.getElementById("indivFL").value;
						if(indivFL=="on")indivFL=false;
						var licFL=false;
						licFL = document.getElementById("licFL").value;
						if(licFL=="on")licFL=false;
						var finFL=false;
						finFL = document.getElementById("finFL").value;
						if(finFL=="on")finFL=false;
						var newCarFL=false;
						newCarFL = document.getElementById("newCarFL").value;
						if(newCarFL=="on")newCarFL=false;
						var usedCarFL=false;
						usedCarFL = document.getElementById("usedCarFL").value;
						if(usedCarFL=="on")usedCarFL=false;
						var bothCarFL=false;
						bothCarFL = document.getElementById("bothCarFL").value;
						if(bothCarFL=="on")bothCarFL=false;
						var newCarSM=false;
						newCarSM = document.getElementById("newCarSM").value;
						if(newCarSM=="on")newCarSM=false;
						var usedCarSM=false;
						usedCarSM = document.getElementById("usedCarSM").value;
						if(usedCarSM=="on")usedCarSM=false;
						var bothCarSM=false;
						bothCarSM = document.getElementById("bothCarSM").value;
						if(bothCarSM=="on")bothCarSM=false;
						var spareAcc=false;
						spareAcc = document.getElementById("spareAcc").value;
						if(spareAcc=="on")spareAcc=false;
						var allAusTrans=false;
						allAusTrans = document.getElementById("allAusTrans").value;
						if(allAusTrans=="on")allAusTrans=false;
						var act=false;
						act = document.getElementById("act").value;
						if(act=="on")act=false;
						var nsw=false;
						nsw = document.getElementById("nsw").value;
						if(nsw=="on")nsw=false;
						var vic=false;
						vic = document.getElementById("vic").value;
						if(vic=="on")vic=false;
						var qld=false;
						qld = document.getElementById("qld").value;
						if(qld=="on")qld=false;
						var tas=false;
						tas = document.getElementById("tas").value;
						if(tas=="on")tas=false;
						var sa=false;
						sa = document.getElementById("sa").value;
						if(sa=="on")sa=false;
						var nt=false;
						nt = document.getElementById("nt").value;
						if(nt=="on")nt=false;
						var wa=false;
						wa = document.getElementById("wa").value;
						if(wa=="on")wa=false;
						var allAusAop=false;
						allAusAop = document.getElementById("allAusAop").value;
						if(allAusAop=="on")allAusAop=false;
						var actAOP=false;
						actAOP = document.getElementById("actAOP").value;
						if(actAOP=="on")actAOP=false;
						var nswAOP=false;
						nswAOP = document.getElementById("nswAOP").value;
						if(nswAOP=="on")nswAOP=false;
						var vicAOP=false;
						vicAOP = document.getElementById("vicAOP").value;
						if(vicAOP=="on")vicAOP=false;
						var qldAOP=false;
						qldAOP = document.getElementById("qldAOP").value;
						if(qldAOP=="on")qldAOP=false;
						var tasAOP=false;
						tasAOP = document.getElementById("tasAOP").value;
						if(tasAOP=="on")tasAOP=false;
						var saAOP=false;
						saAOP = document.getElementById("saAOP").value;
						if(saAOP=="on")saAOP=false;
						var ntAOP=false;
						ntAOP = document.getElementById("ntAOP").value;
						if(ntAOP=="on")ntAOP=false;
						var waAOP=false;
						waAOP = document.getElementById("waAOP").value;
						if(waAOP=="on")waAOP=false;
						var allMakes=false;
						allMakes = document.getElementById("allMakes").value;
						if(allMakes=="on")allMakes=false;
						alert("indiv"+indivFL);
		                var jsonInputToAPI = {
		                	 "password": $('#passwordC').val(),
		                	  "email": $('#emailAddressC').val(),
		                	  "dealername": "g2",
		                	  "website": $('#dWebSite').val(),
		                	  "delmobile": 98,
		                	  "landLine1": 98,
		                	  "landLine2": 98,
		                	  "streetNumber": "ss",
		                	  "streetName": "ss",
		                	  "areaName": $('#dAddress').val(),
		                	  "suborb": "ss",
		                	  "state": "ss",
		                	  "postcode": 98,
		                	  "inventory": null,
		                	  "dealSearch": null,
		                	  "dealSearchInsurance": null,
		                	  "dealSearchFinance": null,
		                	  "dealSearchServMaint": null,
		                	  "dealSearchTransp": null,
		                	  "vehicleQuotation": [],
		                	  "insuranceQuotation": [],
		                	  "financeQuotation": [],
		                	  "vehicleDealerDetails": [{"vehicleType":"car","isNewCar":newCarVehDeal,"isUsedCar":usedCarVehDeal,"isBoth":bothCarVehDeal,"flex1":"  flex1","flex2":"  flex2","flex3":"  flex3","flex4":"  flex4","flex5":0,"flex6":0,"flex7":0,"flex8":0,"flex9":0,"flex10":0,"flex11":"null","flex12":"null","flex13":"false","flex14":"false"}],
											 "vehicleDealerFinanceDetails": [{"vehicleType":"car","individual":indivFL,"licensedBroker":licFL,"financialInstitute":finFL,"isNewCar":newCarFL,"isUsedCar":usedCarFL,"isBoth":bothCarFL,"flex1":"  flex1","flex2":"  flex2","flex3":"  flex3","flex4":"  flex4","flex5":0,"flex6":0,"flex7":0,"flex8":0,"flex9":0,"flex10":0,"flex11":"null","flex12":"null","flex13":"false","flex14":"false"}],
															  "vehicleDealerInsuranceDetails": [{"vehicleType":"car","individual":false,"licensedBroker":false,"financialInstitute":false,"isNewCar":false,"isUsedCar":false,"isBoth":false,"flex1":"  flex1","flex2":"  flex2","flex3":"  flex3","flex4":"  flex4","flex5":0,"flex6":0,"flex7":0,"flex8":0,"flex9":0,"flex10":0,"flex11":"null","flex12":"null","flex13":"false","flex14":"false"}],
																							  "vehicleDealerServMaintDetails": [{"vehicleType":"car","individual":false,"licensedBroker":false,"financialInstitute":false,"isNewCar":newCarSM,"isUsedCar":usedCarSM,"isBoth":bothCarSM,"flex1":"  flex1","flex2":"  flex2","flex3":"  flex3","flex4":"  flex4","flex5":0,"flex6":0,"flex7":0,"flex8":0,"flex9":0,"flex10":0,"flex11":"null","flex12":"null","flex13":"false","flex14":"false"}],
																															  "vehicleDealerMakeList":[{"Make":"Make","offered":true,"flex1":"  flex1","flex2":"  flex2","flex3":"  flex3","flex4":"  flex4","flex5":0,"flex6":0,"flex7":0,"flex8":0,"flex9":0,"flex10":0,"flex11":"null","flex12":"null","flex13":"false","flex14":"false"}],
																																							  "vehicleDealerAreaOfOperState": [{"country":"Australia","state":"state","offered":true,"flex1":"  flex1","flex2":"  flex2","flex3":"  flex3","flex4":"  flex4","flex5":0,"flex6":0,"flex7":0,"flex8":0,"flex9":0,"flex10":0,"flex11":"null","flex12":"null","flex13":"false","flex14":"false"}],
																																															  "vehicleDealerRegion":[{"country":"Australia","state":"state","region":"region","offered":true,"flex1":"  flex1","flex2":"  flex2","flex3":"  flex3","flex4":"  flex4","flex5":0,"flex6":0,"flex7":0,"flex8":0,"flex9":0,"flex10":0,"flex11":"null","flex12":"null","flex13":"false","flex14":"false"}],
																																																							  "vehicleDealerPostCode": [{"country":"Australia","state":"state","region":"region","postCode":1234,"offered":true,"flex1":"  flex1","flex2":"  flex2","flex3":"  flex3","flex4":"  flex4","flex5":0,"flex6":0,"flex7":0,"flex8":0,"flex9":0,"flex10":0,"flex11":"null","flex12":"null","flex13":"false","flex14":"false"}],
		                	  "dealer": true,
		                	  "financer": false,
		                	  "insurer": false,
		                	 "licenseNo" :$('#dLicenseNo').val(),
		                	  "abnnumber": $('#abnNumber').val(),
		                	  "dealGroup":$('#dGroup').val(),
		                	  "acnnumber": "ss",
		                	  "isServMaint":false,
		                		"sparesAccess":spareAcc,
		                		"vehTransProv":false,
		                		"areaOfOperartinAllAus":allAusAop,
		                		"areaOfOperartinStates":false,
		                		"areaOfOperartinPostcodes":false,
		                		"allMakes":false,
		                	  "admin": false
		                	}
		              	                
            
            // table 9
            var tbl9 = document.getElementById('my-table9'), // table reference
               row = tbl9.rows.length;      // append table row
                var i;
                alert("row length relatedModels"+tbl9.rows.length);
                alert("col lenght relatedModels"+tbl9.rows[0].cells.length);         
		                alert("Json print d e relatedModels");
			        
			            // insert table cells to the new row
                for (j = 1; j < tbl9.rows.length; j++) {
                	i=0;
                		//jsonInputToAPI.vehicleDealerPostCode[j-1].vehicleDealAreaOfOperPostId=null;
                		
                		jsonInputToAPI.vehicleDealerPostCode[j-1].country=$('#relatedModels'+j+'0').val();
                		
                		jsonInputToAPI.vehicleDealerPostCode[j-1].state=$('#relatedModels'+j+'1').val(); 
                		jsonInputToAPI.vehicleDealerPostCode[j-1].region=$('#relatedModels'+j+'2').val(); 
                		jsonInputToAPI.vehicleDealerPostCode[j-1].postCode=$('#relatedModels'+j+'3').val(); 
                		jsonInputToAPI.vehicleDealerPostCode[j-1].offered="yes"; 
			         
                		jsonInputToAPI.vehicleDealerPostCode[j-1]["flex1"]="flex1";
		            	jsonInputToAPI.vehicleDealerPostCode[j-1]["flex2"]="flex1";
		            	jsonInputToAPI.vehicleDealerPostCode[j-1]["flex3"]="flex1";
		            	jsonInputToAPI.vehicleDealerPostCode[j-1]["flex4"]="flex1";
		            	jsonInputToAPI.vehicleDealerPostCode[j-1]["flex5"]=0;
		            	jsonInputToAPI.vehicleDealerPostCode[j-1]["flex6"]=0;
		            	jsonInputToAPI.vehicleDealerPostCode[j-1]["flex7"]=0;
		            	jsonInputToAPI.vehicleDealerPostCode[j-1]["flex8"]=0;
		            	jsonInputToAPI.vehicleDealerPostCode[j-1]["flex9"]=0;
		            	jsonInputToAPI.vehicleDealerPostCode[j-1]["flex10"]=0;
		            	jsonInputToAPI.vehicleDealerPostCode[j-1]["flex11"]=null;
		            	jsonInputToAPI.vehicleDealerPostCode[j-1]["flex12"]=null;
		            	jsonInputToAPI.vehicleDealerPostCode[j-1]["flex13"]=false;
		            	jsonInputToAPI.vehicleDealerPostCode[j-1]["flex14"]=false;
			    }  
			                alert("Json print relatedModels");
			                alert(JSON.stringify(jsonInputToAPI.vehicleDealerPostCode));
            
			             // table 91
			                var tbl91 = document.getElementById('my-table91'), // table reference
			                   row = tbl91.rows.length;      // append table row
			                    var i;
			                    alert("row length relatedModels91"+tbl91.rows.length);
			                    alert("col lenght relatedModels91"+tbl91.rows[0].cells.length);         
			    		                alert("Json print d e relatedModels");
			    			        
			    			            // insert table cells to the new row
			                    for (j = 1; j < tbl91.rows.length; j++) {
			                    	i=0;
			                    		jsonInputToAPI.vehicleDealerRegion[j-1].vehicleDealAreaOfOperRegionId=null;
			                    		
			                    		jsonInputToAPI.vehicleDealerRegion[j-1].country=$('#relatedModels91'+j+'0').val();
			                    		
			                    		jsonInputToAPI.vehicleDealerRegion[j-1].state=$('#relatedModels91'+j+'1').val(); 
			                    		jsonInputToAPI.vehicleDealerRegion[j-1].region=$('#relatedModels91'+j+'2').val(); 
			                    		jsonInputToAPI.vehicleDealerRegion[j-1].offered="yes"; 
			    			         
			                    		jsonInputToAPI.vehicleDealerRegion[j-1]["flex1"]="flex1";
			    		            	jsonInputToAPI.vehicleDealerRegion[j-1]["flex2"]="flex1";
			    		            	jsonInputToAPI.vehicleDealerRegion[j-1]["flex3"]="flex1";
			    		            	jsonInputToAPI.vehicleDealerRegion[j-1]["flex4"]="flex1";
			    		            	jsonInputToAPI.vehicleDealerRegion[j-1]["flex5"]=0;
			    		            	jsonInputToAPI.vehicleDealerRegion[j-1]["flex6"]=0;
			    		            	jsonInputToAPI.vehicleDealerRegion[j-1]["flex7"]=0;
			    		            	jsonInputToAPI.vehicleDealerRegion[j-1]["flex8"]=0;
			    		            	jsonInputToAPI.vehicleDealerRegion[j-1]["flex9"]=0;
			    		            	jsonInputToAPI.vehicleDealerRegion[j-1]["flex10"]=0;
			    		            	jsonInputToAPI.vehicleDealerRegion[j-1]["flex11"]=null;
			    		            	jsonInputToAPI.vehicleDealerRegion[j-1]["flex12"]=null;
			    		            	jsonInputToAPI.vehicleDealerRegion[j-1]["flex13"]=false;
			    		            	jsonInputToAPI.vehicleDealerRegion[j-1]["flex14"]=false;
			    			    }  
			    			                alert("Json print relatedModels");
			    			                alert(JSON.stringify(jsonInputToAPI.vehicleDealerRegion));
			    			                
			    			                
			    			                // table 91
			    			                var tbl922 = document.getElementById('my-table922'), // table reference
			    			                   row = tbl922.rows.length;      // append table row
			    			                    var i;
			    			                    alert("row length relatedModels91"+tbl922.rows.length);
			    			                    alert("col lenght relatedModels91"+tbl922.rows[0].cells.length);         
			    			    		                alert("Json print d e relatedModels");
			    			    			        
			    			    			            // insert table cells to the new row
			    			                    for (j = 1; j < tbl922.rows.length; j++) {
			    			                    	i=0;
			    			                    		jsonInputToAPI.vehicleDealerMakeList[j-1].vehicleDealMakeListId=null;
			    			                    		
			    			                    		jsonInputToAPI.vehicleDealerMakeList[j-1].Make=$('#relatedModels91'+j+'0').val();
			    			                    		
			    			                    		jsonInputToAPI.vehicleDealerMakeList[j-1].offered="yes"; 
			    			    			         
			    			                    		jsonInputToAPI.vehicleDealerMakeList[j-1]["flex1"]="flex1";
			    			    		            	jsonInputToAPI.vehicleDealerMakeList[j-1]["flex2"]="flex1";
			    			    		            	jsonInputToAPI.vehicleDealerMakeList[j-1]["flex3"]="flex1";
			    			    		            	jsonInputToAPI.vehicleDealerMakeList[j-1]["flex4"]="flex1";
			    			    		            	jsonInputToAPI.vehicleDealerMakeList[j-1]["flex5"]=0;
			    			    		            	jsonInputToAPI.vehicleDealerMakeList[j-1]["flex6"]=0;
			    			    		            	jsonInputToAPI.vehicleDealerMakeList[j-1]["flex7"]=0;
			    			    		            	jsonInputToAPI.vehicleDealerMakeList[j-1]["flex8"]=0;
			    			    		            	jsonInputToAPI.vehicleDealerMakeList[j-1]["flex9"]=0;
			    			    		            	jsonInputToAPI.vehicleDealerMakeList[j-1]["flex10"]=0;
			    			    		            	jsonInputToAPI.vehicleDealerMakeList[j-1]["flex11"]=null;
			    			    		            	jsonInputToAPI.vehicleDealerMakeList[j-1]["flex12"]=null;
			    			    		            	jsonInputToAPI.vehicleDealerMakeList[j-1]["flex13"]=false;
			    			    		            	jsonInputToAPI.vehicleDealerMakeList[j-1]["flex14"]=false;
			    			    			    }  
			    			    			                alert("Json print relatedModels");
			    			    			                alert(JSON.stringify(jsonInputToAPI.vehicleDealerMakeList));
			                
							             
							            
		             /*   
		                "carModel2": [
		                
		                
									    {
									      "tempCarModelOptFeatId": null,
									      
										
										 "prices":null,
										"optionaFeature":null,
									"flex1": null,
									"flex2" : null,
									 "flex3":null,	
									" flex4" : null,
									"flex5": null,
									"flex6" : null,
									 "flex7" : null,
									 "flex8" : null,
									 "flex9" : null,
									 "flex10" : null,
									 "flex11" : null,
									 "flex12" : null
									    },
		                
		                */
		                
						

						//alert("Before Call");
						//var wsURL = 'http://localhost:8080/MyCarDomain/api/eBid/myVehicle/';
						//var wsURL = 'http://www.autoscoop.com.au/api/eBid/myVehicle/';
						var wsURL = 'api/dealer';
						
							    $http({
											method : 'POST',
											url : wsURL,
											data: jsonInputToAPI
															
										}).success(function(data) {
											alert("Successfully Stored..");
															alert("Thank You. Your MyVehicle ID is " + data.myVehicleId);
																							
														});													
						};

				});

//angular JS end




